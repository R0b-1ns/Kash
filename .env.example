# Security
SECRET_KEY=your-super-secret-key-change-this-in-production

# Database (used by docker-compose, no need to change)
DATABASE_URL=postgresql://finance:finance@postgres:5432/finance_db

# Ollama (le modèle est téléchargé automatiquement au premier démarrage)
OLLAMA_HOST=http://ollama:11434
OLLAMA_MODEL=mistral

# NAS Sync via SMB (optional)
# 1. Monter le partage SMB sur le Mac :
#    mount -t smbfs //user:password@nas-ip/partage /Volumes/NAS
# 2. Configurer les variables :
NAS_LOCAL_PATH=/Volumes/NAS/finance    # Chemin local du montage SMB sur le Mac
NAS_MOUNT_PATH=/app/nas_backup          # Chemin dans le container (ne pas changer)

# OCR Microservice
OCR_SERVICE_URL=http://ocr-service:5001

